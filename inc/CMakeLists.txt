# ======================================================================
#               Main Library
# ======================================================================
add_library(dmboot_inc INTERFACE)

target_include_directories(dmboot_inc INTERFACE 
    ${CMAKE_CURRENT_SOURCE_DIR}
)